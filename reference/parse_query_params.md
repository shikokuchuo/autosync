# Parse query parameters from a URI

Parse query parameters from a URI

## Usage

``` r
parse_query_params(uri)
```

## Arguments

- uri:

  Request URI string (e.g., "/callback?code=abc&state=xyz").

## Value

Named list of decoded query parameters.

# Discover the JWKS URI for an OIDC issuer

Discover the JWKS URI for an OIDC issuer

## Usage

``` r
discover_jwks_uri(issuer)
```

## Arguments

- issuer:

  The OIDC issuer URL.

## Value

Character string, the JWKS URI.

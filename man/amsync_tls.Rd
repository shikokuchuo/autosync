% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{amsync_tls}
\alias{amsync_tls}
\title{Create TLS configuration for secure WebSocket server}
\usage{
amsync_tls(cert_file = NULL, self_signed = FALSE, hostname = "127.0.0.1")
}
\arguments{
\item{cert_file}{Path to PEM file containing certificate and private key,
or a list with \verb{$server} component from \code{\link[nanonext:write_cert]{nanonext::write_cert()}}.}

\item{self_signed}{If TRUE and cert_file is NULL, generates a self-signed
certificate for the specified hostname.}

\item{hostname}{Hostname for self-signed certificate (default "127.0.0.1").}
}
\value{
A TLS configuration object for use with \code{\link[=amsync_server]{amsync_server()}}, or NULL
if neither cert_file nor self_signed is specified.
}
\description{
Creates a TLS configuration for wss:// connections.
}
\examples{
\dontrun{
# Using self-signed certificate
tls <- amsync_tls(self_signed = TRUE, hostname = "localhost")
server <- amsync_server(port = 3030, tls = tls)

# Using existing certificate
tls <- amsync_tls(cert_file = "/path/to/cert.pem")
server <- amsync_server(port = 3030, tls = tls)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{amsync_project}
\alias{amsync_project}
\title{Browse and edit files in a project document}
\usage{
amsync_project(
  url,
  proj_id,
  token = NULL,
  tls = NULL,
  timeout = 5000L,
  files_key = "files"
)
}
\arguments{
\item{url}{WebSocket URL of the sync server (e.g., "ws://localhost:3030/"
or "wss://sync.automerge.org/"). Note: trailing slash may be required.}

\item{proj_id}{Document ID of the project.}

\item{token}{(optional) JWT (ID token) for authenticated servers.
Sent as a Bearer token in the Authorization header of the WebSocket
upgrade request.}

\item{tls}{(optional) for secure wss:// connections to servers with
self-signed or custom CA certificates, a TLS configuration object
created by \code{\link[nanonext:tls_config]{nanonext::tls_config()}}.}

\item{timeout}{Timeout in milliseconds for each receive operation. Default 5000.}

\item{files_key}{Key of the files map within the project document. Default
\code{"files"}.}
}
\value{
An environment of class \code{"amsync_project"} (reference semantics)
with the following fields and methods:
\describe{
\item{\code{doc}}{The fetched project document.}
\item{\code{paths()}}{Current sorted file paths.}
\item{\code{doc_id(path)}}{Resolve a path to its document ID.}
\item{\code{open(path)}}{Return a live \code{\link[=amsync_client]{amsync_client()}} for the file's
document (the caller owns it and must \verb{$close()} it).}
\item{\code{edit(path = NULL)}}{Open the file's client, run \code{\link[=amsync_edit]{amsync_edit()}}
with the extension inferred from the path, then close the client. If
\code{path} is \code{NULL} and interactive, shows the picker first.}
\item{\code{browse()}}{Interactive loop: print the tree, pick a file, edit
it, repeat.}
\item{\code{refresh()}}{Re-fetch the project document to pick up added or
removed files.}
}
}
\description{
Given a sync server URL and a project document ID, fetches the project and
exposes its file tree for browsing and editing. A project is an Automerge
document with a \code{files} map whose keys are file paths and whose values are
text objects holding each file's own document ID.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
proj <- amsync_project("wss://quarto-hub.com/ws", proj_id, token = amsync_token())
proj                                   # prints the file tree
proj$browse()                          # pick a file, edit it, repeat
proj$edit("/charlie/index.qmd")        # edit a known path directly
\dontshow{\}) # examplesIf}
}

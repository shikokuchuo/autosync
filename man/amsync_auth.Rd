% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{amsync_auth}
\alias{amsync_auth}
\title{Get an access token for autosync authentication}
\usage{
amsync_auth(
  email = gargle::gargle_oauth_email(),
  scopes = "https://www.googleapis.com/auth/userinfo.email",
  ...
)
}
\arguments{
\item{email}{Email address to use for authentication, or NULL for
interactive selection. See \code{\link[gargle:gargle_options]{gargle::gargle_oauth_email()}}.}

\item{scopes}{OAuth2 scopes. Defaults to userinfo.email which is sufficient
for identity verification.}

\item{...}{Additional arguments passed to \code{\link[gargle:token_fetch]{gargle::token_fetch()}}.}
}
\value{
Character string containing the access token.
}
\description{
Convenience wrapper around \code{\link[gargle:token_fetch]{gargle::token_fetch()}} that obtains a token
suitable for authenticating with an autosync server.
}
\examples{
\dontrun{
# Interactive authentication
token <- amsync_auth()

# Use with amsync_fetch
doc <- amsync_fetch(
  url = "wss://secure-server.example.com",
  doc_id = "myDocId",
  access_token = token
)

# Specify email for non-interactive use
token <- amsync_auth(email = "user@example.com")
}

}
